# 2026-09-18 review 收口 — PR #46（venue）+ PR #47（option B / 閒置重設）

獨立 review（curl 直連 OpenCode Go relay，唔經 agent loop）：`glm-5.3` ＋ `kimi-k3`。
本檔記錄裁決、收咗嘅項、同**未收**嘅項（唔好當全部做完）。

---

## PR #46 `fix/outcome-venue-spot-only`

**VERDICT（kimi-k3）：APPROVE-WITH-FIXES** — 冇可證實嘅 BLOCKER。兩個 root cause
（local-naive timestamp、venue 混用）都修中、fail-closed 方向啱、defer 優於 book-錯嘅
判斷啱、tp1/trail 唔更新嘅推理成立。

### 已收（本 commit）

- **8(a) 刪走符號嘅外部引用 — 已 grep 全 repo，冇 hit。**
  `_live_spot_price` 喺 `backtest.py:127` 係**自己一份獨立定義**（`backtest.py` 唔
  import `paper_trade`）→ 唔構成 BLOCKER。`scripts/test_review_fixes_20260825.py`
  嘅 hit 係**負向斷言**（`"GC_F_BASIS_FAIL_USD" not in src`），即防止 reintroduction
  嘅守衛。
- **7 空心 pop 測試 — 已修（headline finding）。**
  `test_tv_series_closes_and_is_tagged` 同 `test_venue_warning_cleared_on_clean_tick`
  都由**乾淨狀態**起步 → `pop("venue_warning", None)` 係 no-op、
  `"venue_warning" not in hist` 必然真 → **刪走兩個 pop，成個 suite 照綠**。
  新增 `test_venue_warning_lifecycle_across_ticks`：同一個 log 行兩 tick
  （先 paxg 後 tv），分別驗「平倉路徑 pop」同「仍然 LIVE 路徑 pop」。
  **已用 mutation 證實**：刪兩個 pop → 紅喺 `closed record drops the stale warning`。
- **2 冇死手掣 — 已加 counter + escalation。**
  加 `VENUE_STALE_ALERT_TICKS = 12`（≈1h @5 分鐘 cron）。`venue_warning` 加
  `since`（首次扣起，穩定）＋ `ticks`（累加）＋ 過閾值大聲 `🚨`。
  理由：呢個分支**正正係遮住 TV 死掉嘅地方** —— 09-15 個 `TVInterval.min_30` typo
  就係令每個 tick 靜默跌落 PAXG 幾日冇人發現。Fail-safe 唔等於 fail-silent。
  新增 `test_venue_deferral_counter_escalates`（counter 累加、`since` 唔被覆寫、
  閾值前唔響、閾值後持續響）。

### 未收（follow-up，唔阻 merge，但要記住）

1. **stale 只係 print，唔會令 decisive 失效**（kimi MAJOR）。反例：tvDatafeed 將來
   改成 `utcfromtimestamp()`（naive 但實為 UTC）→ 條 series 老 8h → stale warning
   會着，但 bars 仍然 decisive → phantom close 照 book，仲 tag `close_data_source="tv"`。
   同一個 bug 反方向復活。**✅ 已收（PR #49）** —— 見下面「後續收口」。
2. **DST 機嘅假保證**：`_LOCAL_TZ` 同測試嘅 `LOCAL_OFF` 都係 import 時 capture，
   DST 機跑跨季 bar 會錯 1h 而測試照綠。HKT 無 DST，唔影響本機。
   （測試半邊已修：`pinned_local_tz()` 唔再靠本機 offset；`_LOCAL_TZ` 本身仍係 import 時
   capture —— HKT 無 DST 所以係可接受嘅已知限制。）
3. **`sim.get("data_source", data_source)` 嘅曖昧 fallback**（kimi）：audit 食 sim-level、
   gate 食 fetch-level，寫法本身承認兩者可以 diverge。一行 defense-in-depth：book 之前
   再驗 sim-level。
4. **冇記 deciding bar 嘅 timestamp**（kimi MINOR）。**✅ 已收（PR #49）**。
5. **`_fetch_m30` TV branch missing-column 路徑**（kimi MINOR）：`'datetime'` 唔存在 →
   跳過轉換 → 但照 set `data_source="tv"` → gate 通過 → sim 喺冇 datetime 嘅 frame 上炸。
   修法兩行：column 唔存在就當 fetch 失敗（`bars = None`）等 fallback 接住。
   **後果已收**（PR #49）：讀唔到 bar time 而家當 unverifiable → 扣起平倉，唔會行到 sim 爆炸
   嗰步。根因（return 一個冇 datetime 嘅 frame）仍未修。
6. **`test_paxg_fallback.py`（M15 venue）冇被本 PR 更新**。至於 `backtest.py`：
   **2026-09-18 評估後判定唔改** —— 佢嗰個係 GC=F 期貨 rollover premium 嘅**警告 banner**
   （自己寫明「does not change any simulation logic」），同 `paper_trade.py` 嘅 venue gate
   （邊個 venue 可以判平倉）係**兩件唔同嘅事**，唔係 parity gap；而且 `backtest.py` 唔喺
   cron 路徑（cron script 提及 backtest 0 次）→ 佢錯都唔影響 live 決策。真正 fork 咗嘅係
   `backtest.py` 自己一份 `_live_spot_price()`（唔 import `paper_trade`），但唔急。
7. **`test_tv_interval_members_exist` 只捉 `interval=TVInterval.X` literal 語法形態**；
   `getattr`／subscript／變數傳參繞過。冇 tvdatafeed 嘅 CI 會 SKIP 埋 `found > 0` →
   真空通過。

### 後續收口（PR #49，2026-09-18）

- **① stale TV series 唔再 decisive**：新增 `TV_STALE_MINUTES = 90` ＋ `_series_lag_minutes()`，
  喺 `check_outcomes` 同 venue gate 合流（同一個 `venue_ok` ＋ `venue_warning` ＋ 12-tick
  escalation，唔另起一套）。**Lag 讀唔到（`None`）都當唔 decisive** —— 量唔到就唔可以當啱。
  `_tv_bars_to_utc` 保留原本警告（operator 第一手提示），**刻意唔喺嗰度 gate**：佢係 helper，
  `--backtest` 重播歷史永遠「stale」，gating 會殺死回測。
  - **Live 影響 = 零（實測）**：真 fetch lag **24.6 分鐘**（limit 90，3× 邊際）；
    成個 cron log `TV bars stale` **零命中**。
- **② `close_bar_time`**：兩個 sim 平倉分支（SL/Trail、Timeout）帶出判價嗰條 bar 嘅時間，
  寫入 close record，`last_unverified` 亦加 `bar_time`。`closed_time` 係 wall clock，
  提供咗「邊條 bar 判」之後，先分得出「延遲／補書平倉」同「即時平倉」。
- **測試**：`scripts/test_outcome_venue.py` 32 → **73 checks**。
- **鑑別力（mutation 實證，唔係聲稱）**：拆走 freshness gate → 紅喺
  `test_stale_tv_series_cannot_close` ＋ `test_unreadable_series_times_cannot_close`；
  平倉分支唔帶 `close_bar_time` → 紅喺 `test_close_bar_time_recorded`；
  gate 只擋過去（`abs`→單向）→ 紅喺 `test_future_dated_series_cannot_close`；
  移除 `AttributeError` → 紅喺 `test_lag_probe_survives_weird_frames`；
  拆走 booking invariant → 紅喺 `test_close_bar_before_seed_is_withheld`；
  全斷 feed 回舊行為 → 紅喺 `test_total_fetch_failure_is_recorded_and_escalates`；
  `_tv_bars_to_utc` 變 no-op → 紅喺 `test_real_chain_local_stamps_survive_conversion`
  （即 incident B 復活會被捉到）。

### 第二輪 review（kimi-k3 round-2 ＋ glm-5.3 完整版）收咗嘅項

- **`_series_lag_minutes` 漏捉 `AttributeError`（kimi-k3，已實測可達）**：`.tz_convert`
  喺 object-dtype column 交返嘅 stdlib `datetime` 上會掟 `AttributeError` → 例外穿出會
  **冚咗成個 tick 兼跳過 `venue_warning` 寫入**，即 escalation 靜默永不觸發（正是本 PR 想
  消滅嘅那類失效）。已加 `AttributeError` ＋ 8 款 weird-frame 測試。
- **null 語義係假嘅（kimi-k3 ＋ glm-5.3 獨立撞到）**：`if series_lag is not None:` 令
  「讀唔到」同「唔適用」喺 disk 上一樣（都冇 key），而我段 comment 卻聲稱兩者分得開 ——
  comment 假、`test_unreadable` 亦係假保證（absent 同 null 都過）。修法：**無條件量度**
  （錯 venue 同錯時鐘可以同時成立 —— fallback feed 自己都可以 mis-stamp），
  `lag_minutes` **一定**寫（`None` = 量唔到、數字 = 實測）。
- **警報倒置（glm-5.3）**：全斷 feed（`_fetch_m30` 回 None）以前只 print 一句就 return ——
  冇 record、冇 counter、冇 🚨；**斷得越徹底越靜**。抽出 `_stamp_venue_warning()` 令全斷／
  錯 venue／錯時鐘共用同一 counter，一樣 12 tick 出 🚨。
- **booking-time invariant（glm-5.3）**：判價 bar `<= seed_dt` → 當 unverified 扣起。
  `_simulate_staged_exit` 跳過所有 `bar_dt <= seed_dt`，所以係真 invariant，而佢正正自動化
  PR 自己引嘅 **09-14-01** 事故 —— 以前只係「事後手工對照得出」，而家會自己響。
- **`run_backtest` 加未來方向檢查（glm-5.3）**：只擋 `lag < -90`。「舊」對回測係常態、
  擋咗會殺死所有回測；但「未來」任何模式都唔合法，唔擋就會令 mirror bug 靜靜哋令每個回測
  錯 8h、壞研究結論。
- **命名同可追溯性（glm-5.3）**：`last_unverified.bar_time` → `close_bar_time`（同 closed
  record 一致；舊名令 grep「點決定平倉」漏晒呢批）；時鐘類 reason 加 `source '...'`，
  令 operator 喺 console 追得到邊條 feed。
- **glm-5.3 Q6 核實係我 regex 誤報**：`return \{[^}]*"closed": True[^}]*\}` 被 f-string 個
  `}` 斷開 → 誤報 Timeout 分支冇 `close_bar_time`。實際兩個 `closed:True` 都有；冇第三條
  close 路徑（`bars_held` 只用於報告）。
- **真鏈路測試**（glm-5.3 Q7 缺口）：`_tv_bars_to_utc → _series_lag_minutes` 兩半各自測過
  但從未一齊測 —— conversion 一壞就只會喺 live 🚨 現形。已補，並用 mutation 證實捉得到
  incident B 復活。
- **兩者一致同意唔需要改**：`abs()` 對稱（唯一造得出合法 >90min 未來嘅係本機 clock 落後，
  咁扣起係啱）、`lag is None` fail-closed（有 12-tick 🚨 兜底）、gate 方向正確。
  glm 另外推導：kimi 擔心嘅「每日休市 21:00–22:00 UTC 零 margin」喺本部署唔成立 ——
  cron 排程 `*/5 7-23 * * 1-5`（只平日），而休市窗口（05:00–06:00 HKT）喺 cron 窗口外。

### 第三、四輪 review（kimi-k3 ＋ glm-5.3）：兩者一致**冇 BLOCKER**

- **兩個 reviewer 都獨立捉到同一個自己整出嚟嘅 bug**：booking invariant 原本 fail-**OPEN**
  （`bar_dt is None` 時 short-circuit 放行）＋ `seed_dt is None` 一樣放行 —— 「series clock
  讀唔到就扣起」但「deciding bar／seed 讀唔到就照 book」，同一條問題兩個相反規則。
  已改全 fail-closed，並接上 `_stamp_venue_warning()`（否則持續報不可 book 嘅平倉 =
  **靜默永久唔平倉**，冇止損保護、冇警報）。
- **我自己個新 test 喺 UTC 機係真空**（kimi-k3）：`test_real_chain_local_stamps_survive_conversion`
  用 `datetime.now()` 建 local stamp → UTC 機 no-op conversion 都 `lag≈0` 照過，即「捉到
  incident B 復活」只喺非 UTC 機成立。**同一類自證陷阱之前已喺 `test_tv_tz_alignment.py`
  修過一次，今次又犯。** 已 pin 死 `_LOCAL_TZ`，並**用 `TZ=UTC` 重跑全套 mutation 實證**
  M6 一樣紅。
- **`run_backtest` 兩個殘餘 fail-open**：只擋未來方向、`lag is None` 照跑；已改 refuse。
- **🚨 文案硬編碼診斷**：counter 而家同時載 booking-invariant 扣起（問題喺 sim／booking，
  唔喺 feed），硬編碼「series venue/時鐘有問題」會令 operator 查錯子系統 → 改由 `reason` 講。
- **`test_total_fetch_failure` 弱 assertion**（`warn.get(...) is None` 對「key 缺席」都過）
  → 收緊成同 `test_unreadable` 一致嘅強形式。
- **kimi Q7(d) 核實**：`_counts_toward_r` 用 `record.get("verified") is not False`（唔靠新
  欄位）；`xauusd_report.py` 同 `scripts/test_paxg_fallback.py` 對新欄位**零 hit**。
- **兩個都同意唔阻塞 merge 嘅 out-of-scope follow-up**：BTC vendored `paper_trade.py` 未
  port 同一 incident class；`seed_trades` 寫 `seeded_time` 本身冇 clock gate；loop 中途
  exception 會令已 stamp 嘅 counter 未 `save_log` 就丟（pre-existing）。
- 最終：**85 checks**、**11 個 mutation（M1–M11）全部被指定測試捉到**、HKT 同 UTC 兩個時區
  結果一致；`bash run_tests.sh` 22/22；3 個 live state sha 前後一致。

### 已核實，唔需要動作

- **8(b) 殘留 paxg-derived `sim_state`**：查 live ledger —— 正統帳簿 `LIVE: none`、
  `CLOSED (1)`（09-14 嗰筆 72 bars 正常收，`last_bar_time` 2026-09-16T02:30Z 係佢自己
  嘅平倉時間，唔係卡住嘅活倉）；`tp1_hit=False`、`trail_active=False`、`trail_stop=None`
  → **冇 phantom BE/Trail**。馬丁帳簿 `open=None`。冇嘢要洗。
- **venue gate 從未觸發**：`xauusd_cron.log` 入面 `non-spot venue` **零次** ——
  （截至 09-18）TV 一直正常，gate 冇扣起過任何平倉。
- 大小寫 / `None` / `""` / 未知字串 → 全部 `False`，fail-closed；`data_source` 係
  `_fetch_m30` 內部 literal，外部注入唔到 → 冇繞道。

---

## PR #47 第三輪修（`_maybe_stale_level_reset` / `_env_float`）

兩輪 reviewer 睇嘅係**修正前**嘅 code，所以修正本身另跑一輪 delta review。

**VERDICT（kimi-k3）：APPROVE-WITH-FIXES**。glm-5.3 嘅 output 係 reasoning dump（32k
budget 燒晒，`finish_reason=length`，無最終 VERDICT），但內容同 kimi **獨立撞出同樣兩個
問題**，另多一個 case-7 flake。

### 已收

- **`_env_float` 值域**（kimi MINOR / glm MAJOR，兩邊一致）：`float()` 擋唔到非有限值 ——
  `nan` 會 parse，而對 nan 嘅比較全部係 False → `hours < nan` 永假、`nan <= 0` 又唔算
  「關閉」→ **每次檢查都 release，加碼序列靜默廢除**（永遠 base size）；`inf`／`1e400`
  係鏡像（規則永久靜默）；負數係 sign typo。而家全部 warn + 退回預設。
  加 11 個值域 case（`nan`／`NaN`／`.nan`／`inf`／`Infinity`／`1e400`／`-14`／`14h`／
  `""`／`0`／`21.5`）。
- **`(now - stamp)` 搬入 try**（kimi）：同「唔可以令檢查搞死交易」嘅承諾一致；
  `_mart_elapsed_minutes` 已經係咁做。
- **over-held 警告分支 —— 刪走（死碼），改為記錄不變式**（kimi + glm 一致要求）。
  **實測證不可達**：路徑 A（held ≥ 10 分鐘 + 有效價）→ 平倉步平咗 → 1b 見 `open=None`；
  路徑 B（同樣持倉 + `price=None`）→ 平倉步 `return st` → 1b 根本唔行。即係 1b 見到活倉
  必然 held < `MART_HOLD_MINUTES`。保留 `return False`（必要），加註釋講明不變式同
  「壞就修 step 1」。新增 case 13/14 釘住呢個不變式。
- **測試凍結時鐘**（glm + kimi 一致）：`pt._mart_now` monkeypatch 成固定時間，所有
  fixture 相對佢計。原本 case 7 用 2 分鐘持倉對 10 分鐘閾值 → **CI 凍結 >8 分鐘就會 flake**。
- **nits**：case 8 註釋（fixture 係 stale 唔係 fresh）、case 5 docstring（over-side 係
  7.2 秒，唔係「hours」）、常數註釋 `19.2%` → `19.1%`（2.25/11.75 = 19.149%）、
  註明 case 4/7 係 white-box fixture（production 唔會自然產生嗰個 state）。

### 測試鑑別力（已用 mutation 證實，唔係聲稱）

| mutation | 結果 |
|---|---|
| `_env_float` 嘅 finite guard 改成 `if False:` | 紅喺 `MART_STALE_LEVEL_HOURS='nan' → 'nan', want 14.0` |
| 拆走「持倉唔准重設」guard | 紅喺 `a live position must block the release` |
| 刪走兩個 `trade.pop("venue_warning", None)` | 紅喺 `closed record drops the stale warning` |

### 未收（follow-up）

- `stale_level_no_basis` 係**每 tick 計**（持續 corruption 會累加，唔似
  `last_no_setups_bar` 嘅 per-bar dedupe）；counter 純顯示，唔影響交易。
- 「14h 對 **gated** cadence 下幾耐先算唔連貫」**冇數據支持** —— 係判斷 call，
  comment 已披露。樣本係 UNGATED cadence、冇週末、n=26。
- PR #46 未收嘅 1–7 項（見上）。

---

## 部署注意（本次部署）

live 之前同 main 分歧：live `paper_trade.py` == PR #46 branch tip（未 merge），main 冇
`3d19b93`（tv tz）同 `d425af9`（venue）。**直接 cp main 版會倒退呢兩個修正**（重新引入
8h 位移同 PAXG 錯價判平倉）。所以先合併 PR #46 入 main，等 main = live + option B，
再部署。

`analyze_v3.py`／`xauusd_report.py`／`backtest.py` 三個 production 檔喺分歧期間一直同
main 一致，唔受影響。
