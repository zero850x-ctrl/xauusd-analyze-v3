# 2026-09-18 review 收口 — PR #46（venue）+ PR #47（option B / 閒置重設）

獨立 review（curl 直連 OpenCode Go relay，唔經 agent loop）：`glm-5.3` ＋ `kimi-k3`。
本檔記錄裁決、收咗嘅項、同**未收**嘅項（唔好當全部做完）。

---

## PR #46 `fix/outcome-venue-spot-only`

**VERDICT（kimi-k3）：APPROVE-WITH-FIXES** — 冇可證實嘅 BLOCKER。兩個 root cause
（local-naive timestamp、venue 混用）都修中、fail-closed 方向啱、defer 優於 book-錯嘅
判斷啱、tp1/trail 唔更新嘅推理成立。

### 已收（本 commit）

- **8(a) 刪走符號嘅外部引用 — 已 grep 全 repo，冇 hit。**
  `_live_spot_price` 喺 `backtest.py:127` 係**自己一份獨立定義**（`backtest.py` 唔
  import `paper_trade`）→ 唔構成 BLOCKER。`scripts/test_review_fixes_20260825.py`
  嘅 hit 係**負向斷言**（`"GC_F_BASIS_FAIL_USD" not in src`），即防止 reintroduction
  嘅守衛。
- **7 空心 pop 測試 — 已修（headline finding）。**
  `test_tv_series_closes_and_is_tagged` 同 `test_venue_warning_cleared_on_clean_tick`
  都由**乾淨狀態**起步 → `pop("venue_warning", None)` 係 no-op、
  `"venue_warning" not in hist` 必然真 → **刪走兩個 pop，成個 suite 照綠**。
  新增 `test_venue_warning_lifecycle_across_ticks`：同一個 log 行兩 tick
  （先 paxg 後 tv），分別驗「平倉路徑 pop」同「仍然 LIVE 路徑 pop」。
  **已用 mutation 證實**：刪兩個 pop → 紅喺 `closed record drops the stale warning`。
- **2 冇死手掣 — 已加 counter + escalation。**
  加 `VENUE_STALE_ALERT_TICKS = 12`（≈1h @5 分鐘 cron）。`venue_warning` 加
  `since`（首次扣起，穩定）＋ `ticks`（累加）＋ 過閾值大聲 `🚨`。
  理由：呢個分支**正正係遮住 TV 死掉嘅地方** —— 09-15 個 `TVInterval.min_30` typo
  就係令每個 tick 靜默跌落 PAXG 幾日冇人發現。Fail-safe 唔等於 fail-silent。
  新增 `test_venue_deferral_counter_escalates`（counter 累加、`since` 唔被覆寫、
  閾值前唔響、閾值後持續響）。

### 未收（follow-up，唔阻 merge，但要記住）

1. **stale 只係 print，唔會令 decisive 失效**（kimi MAJOR）。反例：tvDatafeed 將來
   改成 `utcfromtimestamp()`（naive 但實為 UTC）→ 條 series 老 8h → stale warning
   會着，但 bars 仍然 decisive → phantom close 照 book，仲 tag `close_data_source="tv"`。
   同一個 bug 反方向復活。建議喺 `check_outcomes` 層拒絕（唔好喺 helper，否則 backtest
   全死）。**另加** `lag < 0` 嘅未來 bar 對稱檢查。
2. **DST 機嘅假保證**：`_LOCAL_TZ` 同測試嘅 `LOCAL_OFF` 都係 import 時 capture，
   DST 機跑跨季 bar 會錯 1h 而測試照綠。HKT 無 DST，唔影響本機。
3. **`sim.get("data_source", data_source)` 嘅曖昧 fallback**（kimi）：audit 食 sim-level、
   gate 食 fetch-level，寫法本身承認兩者可以 diverge。一行 defense-in-depth：book 之前
   再驗 sim-level。
4. **冇記 deciding bar 嘅 timestamp**（kimi MINOR）：`close_bar_time` 一行。而家
   `closed_time` 係 wall clock，TV 死兩日後恢復 → book 一個用兩日前 bar 嘅 close，
   張 record 同即時 close 冇分別（09-14-01 嗰單正正靠呢個對照先揭到）。
5. **`_fetch_m30` TV branch missing-column 路徑**（kimi MINOR）：`'datetime'` 唔存在 →
   跳過轉換 → 但照 set `data_source="tv"` → gate 通過 → sim 喺冇 datetime 嘅 frame 上炸。
   修法兩行：column 唔存在就當 fetch 失敗（`bars = None`）等 fallback 接住。
6. **`test_paxg_fallback.py`（M15 venue）冇被本 PR 更新**，同埋 `backtest.py` 仍然用舊嘅
   gold-api basis check（同 `paper_trade.py` 嘅新政策唔再一致）→ parity gap。
7. **`test_tv_interval_members_exist` 只捉 `interval=TVInterval.X` literal 語法形態**；
   `getattr`／subscript／變數傳參繞過。冇 tvdatafeed 嘅 CI 會 SKIP 埋 `found > 0` →
   真空通過。

### 已核實，唔需要動作

- **8(b) 殘留 paxg-derived `sim_state`**：查 live ledger —— 正統帳簿 `LIVE: none`、
  `CLOSED (1)`（09-14 嗰筆 72 bars 正常收，`last_bar_time` 2026-09-16T02:30Z 係佢自己
  嘅平倉時間，唔係卡住嘅活倉）；`tp1_hit=False`、`trail_active=False`、`trail_stop=None`
  → **冇 phantom BE/Trail**。馬丁帳簿 `open=None`。冇嘢要洗。
- **venue gate 從未觸發**：`xauusd_cron.log` 入面 `non-spot venue` **零次** ——
  （截至 09-18）TV 一直正常，gate 冇扣起過任何平倉。
- 大小寫 / `None` / `""` / 未知字串 → 全部 `False`，fail-closed；`data_source` 係
  `_fetch_m30` 內部 literal，外部注入唔到 → 冇繞道。

---

## PR #47 第三輪修（`_maybe_stale_level_reset` / `_env_float`）

兩輪 reviewer 睇嘅係**修正前**嘅 code，所以修正本身另跑一輪 delta review。

**VERDICT（kimi-k3）：APPROVE-WITH-FIXES**。glm-5.3 嘅 output 係 reasoning dump（32k
budget 燒晒，`finish_reason=length`，無最終 VERDICT），但內容同 kimi **獨立撞出同樣兩個
問題**，另多一個 case-7 flake。

### 已收

- **`_env_float` 值域**（kimi MINOR / glm MAJOR，兩邊一致）：`float()` 擋唔到非有限值 ——
  `nan` 會 parse，而對 nan 嘅比較全部係 False → `hours < nan` 永假、`nan <= 0` 又唔算
  「關閉」→ **每次檢查都 release，加碼序列靜默廢除**（永遠 base size）；`inf`／`1e400`
  係鏡像（規則永久靜默）；負數係 sign typo。而家全部 warn + 退回預設。
  加 11 個值域 case（`nan`／`NaN`／`.nan`／`inf`／`Infinity`／`1e400`／`-14`／`14h`／
  `""`／`0`／`21.5`）。
- **`(now - stamp)` 搬入 try**（kimi）：同「唔可以令檢查搞死交易」嘅承諾一致；
  `_mart_elapsed_minutes` 已經係咁做。
- **over-held 警告分支 —— 刪走（死碼），改為記錄不變式**（kimi + glm 一致要求）。
  **實測證不可達**：路徑 A（held ≥ 10 分鐘 + 有效價）→ 平倉步平咗 → 1b 見 `open=None`；
  路徑 B（同樣持倉 + `price=None`）→ 平倉步 `return st` → 1b 根本唔行。即係 1b 見到活倉
  必然 held < `MART_HOLD_MINUTES`。保留 `return False`（必要），加註釋講明不變式同
  「壞就修 step 1」。新增 case 13/14 釘住呢個不變式。
- **測試凍結時鐘**（glm + kimi 一致）：`pt._mart_now` monkeypatch 成固定時間，所有
  fixture 相對佢計。原本 case 7 用 2 分鐘持倉對 10 分鐘閾值 → **CI 凍結 >8 分鐘就會 flake**。
- **nits**：case 8 註釋（fixture 係 stale 唔係 fresh）、case 5 docstring（over-side 係
  7.2 秒，唔係「hours」）、常數註釋 `19.2%` → `19.1%`（2.25/11.75 = 19.149%）、
  註明 case 4/7 係 white-box fixture（production 唔會自然產生嗰個 state）。

### 測試鑑別力（已用 mutation 證實，唔係聲稱）

| mutation | 結果 |
|---|---|
| `_env_float` 嘅 finite guard 改成 `if False:` | 紅喺 `MART_STALE_LEVEL_HOURS='nan' → 'nan', want 14.0` |
| 拆走「持倉唔准重設」guard | 紅喺 `a live position must block the release` |
| 刪走兩個 `trade.pop("venue_warning", None)` | 紅喺 `closed record drops the stale warning` |

### 未收（follow-up）

- `stale_level_no_basis` 係**每 tick 計**（持續 corruption 會累加，唔似
  `last_no_setups_bar` 嘅 per-bar dedupe）；counter 純顯示，唔影響交易。
- 「14h 對 **gated** cadence 下幾耐先算唔連貫」**冇數據支持** —— 係判斷 call，
  comment 已披露。樣本係 UNGATED cadence、冇週末、n=26。
- PR #46 未收嘅 1–7 項（見上）。

---

## 部署注意（本次部署）

live 之前同 main 分歧：live `paper_trade.py` == PR #46 branch tip（未 merge），main 冇
`3d19b93`（tv tz）同 `d425af9`（venue）。**直接 cp main 版會倒退呢兩個修正**（重新引入
8h 位移同 PAXG 錯價判平倉）。所以先合併 PR #46 入 main，等 main = live + option B，
再部署。

`analyze_v3.py`／`xauusd_report.py`／`backtest.py` 三個 production 檔喺分歧期間一直同
main 一致，唔受影響。
